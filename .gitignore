# Local env
.env
.env.local

# Dependencies
node_modules/

# Vercel CLI local state (contains projectId/orgId — should stay local)
.vercel/

# Editor / local tooling
.claude/
.DS_Store
*.log

# Build artifacts (none for this static site, but just in case)
.next/
dist/
